# frontend-vite

Minimal Vite + React frontend that fetches registers from the modular monolith.

Run:
1. cd frontend-vite
2. npm install
3. npm run dev

It expects the backend monolith running on http://localhost:8080
