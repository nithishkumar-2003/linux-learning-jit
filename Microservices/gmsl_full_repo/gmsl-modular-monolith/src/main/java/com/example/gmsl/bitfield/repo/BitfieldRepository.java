package com.example.gmsl.bitfield.repo;

import com.example.gmsl.bitfield.model.Bitfield;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BitfieldRepository extends JpaRepository<Bitfield, Long> {}
